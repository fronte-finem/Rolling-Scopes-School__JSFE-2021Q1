extends ../../layout/_layout-page.pug

include ../../block/_side-bar.pug
include ../../block/_live-cams.pug
include ../../block/_quick-donation.pug
include ../../block/_facts.pug

block page-vars
  - var page = "zoos";
  - var file = "zoos";
  - var title = '🐼';
  - var root = '../..'
  - var animal = 'panda'

block main
  +side-bar(root, animal)
  +live-cams(root, animal)
  +quick-donation(root, animal)
  +facts(root, animal)

//- block dev
//-   .dev-overlay.dev-grid
