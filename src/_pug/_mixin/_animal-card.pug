include ./_button.pug

mixin animal-card(cls, animal)
  .animal-card(class = cls)
    figure.card-pict
      img.animal-photo(src=animal.imgSrc, alt=`${animal.kind} — ${animal.name}`)
      figcaption.animal-name= animal.name
    .card-info
      section.animal-info
        h4.animal-kind= animal.kind
        p.animal-descr= animal.descr
      +btn('a', 'view live cam', 'text')
