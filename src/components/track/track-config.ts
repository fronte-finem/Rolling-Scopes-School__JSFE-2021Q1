export enum TrackEvent {
  SELECT = 'select',
  REMOVE = 'remove',
  START = 'start',
  STOP = 'stop',
}

export enum TrackButton {
  SELECT = 'select',
  REMOVE = 'remove',
  START = 'A',
  STOP = 'B',
}

export enum TrackState {
  INITIAL = 'initial',
  DRIVE = 'drive',
  EMPTY = 'empty',
}
