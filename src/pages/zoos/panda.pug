extends ../../layout/_layout-page.pug

include ../../block/_side-bar.pug

block page-vars
  - var page = "zoos";
  - var file = "zoos";
  - var title = '🐼';
  - var root = '../..'

block main
  +side-bar('panda')

block dev
  .dev-overlay.dev-grid
