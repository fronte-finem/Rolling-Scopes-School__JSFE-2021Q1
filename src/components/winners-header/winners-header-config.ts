export enum WinnersHeaderName {
  NUM = 'number',
  CAR = 'car',
  NAME = 'name',
  WINS = 'wins',
  TIME = 'best time (seconds)',
}

export enum WinnersHeaderState {
  INITIAL = 'initial',
  SORT_WINS = 'sort wins',
  SORT_TIME = 'sort time',
}
