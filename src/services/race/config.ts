export enum GarageEvent {
  GARAGE_ERROR,
  EMPTY,
}

export enum RaceEvent {
  GARAGE_ERROR,
  RACE_WIN,
  RACE_END,
  START,
  STOP,
  EMPTY,
}

export enum WinnersEvent {
  WINNERS_ERROR,
  EMPTY,
}
