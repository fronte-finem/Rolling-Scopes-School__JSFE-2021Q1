export enum GameSoundEffect {
  YES = './sfx/yes.mp3',
  NO = './sfx/no.mp3',
  WIN = './sfx/win.mp3',
  FAIL = './sfx/fail.mp3',
}

export enum GameStatus {
  INITIAL = 'initial',
  READY = 'ready',
  START = 'start',
  END = 'end',
  VOCALIZE = 'vocalize',
  MATCHING = 'matching',
  HIT = 'hit',
  MISS = 'miss',
  SHOW_RESULT = 'show result',
}
